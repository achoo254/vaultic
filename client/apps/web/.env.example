# Vaultic Web App — environment variables
# Copy to .env and adjust values for your environment

# API base URL — backend server address
# Dev: leave empty (Vite proxy handles it via vite.config.ts)
# Production: set to your API domain (e.g. https://api.vaultic.io)
VITE_API_URL=
