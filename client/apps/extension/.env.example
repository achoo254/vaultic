# Vaultic Extension — Environment Variables
# Copy to .env or .env.dattqh and adjust values

# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8080

# Share page base URL (where recipients view shared secrets)
VITE_SHARE_URL=http://localhost:8080/s
